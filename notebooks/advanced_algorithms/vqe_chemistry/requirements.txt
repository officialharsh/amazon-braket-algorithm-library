# Additional open-source dependencies for the VQE chemistry notebook, on top of the
# amazon-braket-algorithm-library install. All are Apache-2.0 / BSD / LGPL compatible.
pennylane>=0.40
amazon-braket-pennylane-plugin>=1.30
pennylane-qchem>=0.40
scipy>=1.10
matplotlib>=3.7
# Optional, only for the covalent (thia-Michael) drug-binding example geometry build:
#   pyscf>=2.5
#   rdkit>=2024.3
